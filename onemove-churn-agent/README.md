# onemove-churn-agent

원무브 회원 이탈 위험 감지 & 대응 에이전트. 설계 배경은 `naver-rank-tracker` 저장소의
`docs/churn-risk-agent-design.md` 참고.

## 진행 단계

- [x] 1단계 — 장난감 예제 (`01-toy-example.js`): 가짜 회원 데이터로 "도구함 + 목표 → AI가 스스로
  판단"하는 에이전트 루프를 확인
- [x] 2단계 — 읽기 전용 도구를 진짜 원무브 데이터에 연결 (`02-real-data-readonly.js`): 실제
  API로 첫 실행 성공 (2026-08-19). `recentVisits`/`avgVisits` 같은 단순 필드가 실제 API엔 없어서,
  AI가 `joinedAt`/`attendedAt`/패스 유효기간(`startedAt`~`endedAt`)을 스스로 비교하는 방식으로
  판단 기준을 즉석에서 만들어냄 — 하드코딩된 로직이 아니라는 걸 실데이터로 확인.

  **실행 노트**: 첫 실행에서 `noVisitsPeriod=14` 후보가 전부 "체험권만 받고 정식 등록 안 한
  사람"이었음 (일주일 체험권 등). 이건 "이탈"이 아니라 "체험 미전환"이라 성격이 다름 → 시스템
  프롬프트/도구 설명에 "정식(비체험) 계약 이력이 있는 회원만 대상"으로 필터링 지시 추가함.
  체험 미전환자 팔로업은 추후 별도 과제로 분리 고려. 또한 `flag_risky_member`에 `reason` 필드가
  누락된 채 호출된 사례가 있어 `strict: true`로 필수값 강제 처리함.

  `/members` 응답이 페이지네이션되어 있어(total 282, 페이지당 15) 처음엔 첫 페이지만 보고 있었음
  → `reservationApi.js`에서 offset을 늘려가며 끝까지 모아오도록 수정. 282명 전체를 검토하면서
  생각(thinking)에 토큰을 많이 써서 `max_tokens` 8000에서 응답 없이 끊기는 문제 발생 →
  스트리밍 + `max_tokens: 16000` + `effort: "medium"`으로 해결.

  282명 전체 실행 결과(1차): 158명 체험 미전환(제외), 80명 신고, 20명은 "레뷰 체험단" 패스라 AI가
  스스로 판단 보류(강석 확인 요청) — **데이터에 없는 걸 추측하지 않고 사람에게 물어본 좋은 사례**.
  확인 결과: 레뷰체험단은 기간이 2주면 최초 신청 시 발급되는 체험용(제외), 3개월권처럼 길면
  정식 유료 회원(포함)이라는 규칙 확인 → 프롬프트에 반영. 또한 80명이 "패스 유효기간 남았는데
  안 옴(지금 잡아야 함)"부터 "10개월 전에 이미 떠남(사실상 이탈 완료)"까지 긴급도가 섞여 있어
  실무에서 우선순위 판단이 안 됨 → `flag_risky_member`에 `risk_level`(high/medium/low) 필드
  추가해서 긴급도별로 구분되게 함.

  **282명 전체 실행 결과(2차)**: risk_level 반영 후 재실행. 총 103명 신고 (high 33 / medium 17 /
  low 53), 체험 미전환 179명 제외. 레뷰체험단 규칙도 정확히 적용됨(117일 '레뷰연계'는 정식 포함,
  14~20일짜리는 체험 제외). 애매한 케이스(예: 패스가 아직 시작 전인 state 0 회원)도 근거를 들어
  스스로 medium으로 분류하고 요약에 설명함.

  강석 피드백: '코치수강권'은 코치(직원)용이라 완전 제외돼야 하고, '엠버서더 전용'/'양도'
  같은 특수 패스는 AI가 좀 더 판단해줬으면 함 → 프롬프트에 반영.

  **282명 전체 실행 결과(3차, 최종 — 2026-08-19)**: 코치수강권 보유자 3명(김지훈/권준학/이기현)
  완전 제외 확인. 엠버서더 전용(8명)/양도(4명)는 신고하되 각 reason에 "유료 고객 관계 확신 낮음,
  컨택 전 결제 이력 확인 필요"를 명시 — 요청한 수준의 판단이 정확히 구현됨. 덤으로 AI가
  `noVisitsPeriod` 후보 중 실제로는 최근 14일 내 `attendedAt`이 있는 28명을 스스로 재검증해서
  오탐으로 제외함 (API 필터만 믿지 않고 실제 필드로 교차검증). 최종: 총 78명 신고
  (high 17 / medium 13 / low 48).

  강석 요청: 수강권-결제내역 대조, 홀딩(휴회) 여부 판단 추가 → `get_transactions_by_month` 도구
  추가, 홀딩 필드는 데이터에서 직접 찾아보고 없으면 솔직히 보고하도록 지시.

  **282명 전체 실행 결과(4차)**: 결제내역 대조로 "엠버서더 전용 = 0원 무상 등록"이라는 실제 근거
  확보, 홀딩 전용 필드는 못 찾았다고 정직하게 보고(추측으로 제외하지 않음). 하지만 신고 인원이
  78→99명으로 튐 — 원인 확인 결과 **AI가 "오늘 날짜"를 몰라서 데이터 안 최근 attendedAt을 오늘로
  착각**, 실행마다 판단이 오락가락하던 버그였음 → `todayStr()`로 실제 오늘 날짜(KST)를 프롬프트에
  직접 주입해서 해결. 또한 결제내역에서 BONG이 트레이너(직원)로 등장하는 걸 발견 → 강석 확인:
  코치수강권처럼 완전 제외 규칙 적용하기로 함.

  **비용 이슈**: 282명 전체를 여러 번 반복 실행하면서 토큰 사용량이 빠르게 늘어남 — 매 턴 전체
  대화(282명 데이터 포함)를 통째로 재전송하는 구조가 원인. 대응: (1) `cache_control`로 프롬프트
  캐싱 활성화, (2) 턴마다 토큰 사용량 로그 추가, (3) 개발 중 `TEST_MEMBER_LIMIT` 환경변수로 후보
  수를 줄여 저렴하게 테스트 가능하게 함, (4) 모델을 `claude-opus-5`에서 `claude-sonnet-5`로 변경
  (강석 확인 — 최종 검증 시 필요하면 opus로 다시 바꿀 수 있음).

  **다음에 재검증할 것**: 오늘 날짜 버그 수정 + BONG 제외 규칙 반영 후 282명 전체로 최종 1회 더
  돌려서 결과 확정할 것 — 3단계(메시지 초안 생성)로 넘어가기 전 마지막 확인.

  강석 피드백: "결과가 알아보기 쉽게 나와야 한다"는 방향만 주고 구체적 형태는 안 정했는데, 확인
  없이 CSV로 바로 구현해버림 → 이 피드백 자체로 `CLAUDE.md`에 "방향만 받았을 때 먼저 확인 질문할
  것" 지침 추가. 이후 다시 질문해서 확정: 결과는 CSV 대신(추가로) **슬랙 메시지로 바로 전송**,
  분량은 "요약 + high 등급 전체 명단"으로. `lib/slack.js` 추가(naver-rank-tracker의
  reporter/slack.js와 동일한 Incoming Webhook 패턴). `SLACK_WEBHOOK_URL` 설정 필요.

  강석 추가 요청: (1) CSV를 팀 전체가 접근 가능하게(구글 드라이브 등), (2) 슬랙 메시지를 전문
  용어 없이 쉽게(예: "박세미님 — 1개월 기간권 보유 중, 20일째 미방문"), (3) 연락한 사람을
  대화하듯 표시하면 다음부터 자동 제외. 확인 질문 후 결정: **CSV → 구글 시트로 전환**,
  연락 여부는 **시트에 체크박스**로 표시하면 다음 실행부터 코드가 자동 필터링. `lib/sheets.js`
  추가 — 시트 자동 초기화(헤더+체크박스 서식), 연락완료 회원ID 조회, 신규 결과 append.
  `flag_risky_member`에 `member_id`/`member_name`/`plain_summary` 필드 추가(기존 `member_label`
  대체) — `plain_summary`는 내부 용어 없는 한 문장, `reason`은 기존처럼 상세 근거(시트에는
  기록되지만 슬랙에는 노출 안 함). CSV/output 방식은 폐기.

  강석 확인: 구글 클라우드 콘솔 + 서비스 계정 방식이 유료인지, 꼭 필요한지 질문 → 요금은
  무료이지만(Sheets API 자체가 무료) 초보자에게 콘솔 UI가 복잡한 건 사실이라 확인 질문 후
  **앱스 스크립트(Apps Script) 웹 앱 방식으로 전환** — 슬랙 웹훅과 똑같이 URL 하나로 통신,
  구글 클라우드 콘솔/서비스 계정/API 키 전부 불필요. `lib/sheets.js`를 googleapis 기반에서
  fetch 기반으로 다시 작성, `apps-script/Code.gs` 추가(사용자가 구글 시트 편집기에 직접
  붙여넣어 배포). `.env`도 `GOOGLE_SERVICE_ACCOUNT_KEY_FILE`/`GOOGLE_SHEET_ID` 대신
  `GOOGLE_APPS_SCRIPT_URL`/`GOOGLE_SHEET_VIEW_URL`로 변경.

  강석이 직접 배포하며 겪은 이슈들: (1) `.env`에 URL을 괄호와 함께 붙여넣어서 `fetch`가 URL
  파싱 실패 → 괄호 제거로 해결(예시 문구를 준 내 잘못, 다음부턴 순수 값만 예시로 줄 것).
  (2) 앱스 스크립트 배포 시 "액세스 권한이 있는 사용자"를 "모든 사용자"로 안 해서 401 →
  배포 관리에서 재설정. 이후 실제 기록 성공 확인(터미널에 "구글 시트에 1명 기록 완료" +
  시트에 실제 행 추가됨). **2단계 최종 검증 완료.**
- [x] 3단계 — 메시지 초안 생성 도구 추가: `draft_outreach_message` 도구 추가. 범위는 강석
  확인 후 **high 등급만**(비용 절감), 출력 위치는 **구글 시트 새 컬럼**("메시지초안", 연락완료
  체크박스 앞)으로 결정. 가드레일: 데이터에 없는 개인 사정(부상/여행 등) 추측 금지, 직원이
  그대로 복사해서 카톡/문자로 보낼 수 있는 짧고 다정한 톤. `apps-script/Code.gs`도 컬럼 구조
  변경(G열이 연락완료로 밀림, 헤더는 매번 덮어써서 기존 시트도 자동 마이그레이션됨).

  **버그 발견 및 수정**: 강석이 실행 후 시트를 열어보니 헤더(1행)만 있고 2~31행이 비어있는데,
  터미널 로그에는 "구글 시트에 1명 기록 완료"라고 떠서 불일치 발생. Cmd+F로 회원명을 검색해보니
  실제 데이터는 **1000번째 줄 근처**에 가 있었음. 원인: 초기 설정 코드가 연락완료 체크박스
  서식을 미리 1000행 범위에 통째로 입혀놨는데, 구글 시트의 `getLastRow()`는 서식만 입혀진
  빈 셀도 "내용 있음"으로 착각해서 실제 마지막 데이터 행을 1000번째 부근으로 잘못 계산 →
  새 데이터가 그 뒤에 이어붙여짐. `apps-script/Code.gs` 수정: `getLastRow()` 대신 A열(날짜)을
  직접 스캔해서 진짜 마지막 데이터 행을 찾는 `findLastDataRow_()` 함수로 교체, 체크박스 서식도
  미리 넓게 깔지 않고 방금 쓴 행에만 입히도록 변경(`applyCheckboxFormat_()`). **아직 강석
  재배포 및 실행 검증 전** — 기존 시트에 남아있는 1000번째 줄 근처의 실제 데이터 행과 그 사이
  빈 행들은 재배포 후 정리 필요.

  **구글 시트 연동 보류**: 위 버그를 고치고 새 배포까지 만들어봤지만, 앱스 스크립트 실행 기록에는
  매번 "완료됨"으로 뜨는데도 외부(curl, Node fetch 둘 다)에서 결과를 받아오는 마지막 단계에서
  계속 "파일을 열 수 없습니다" 에러가 남. 배포 액세스 권한("모든 사용자"), 시트 공유 설정("링크가
  있는 모든 사용자"), 권한 승인 여부까지 다 확인했지만 원인을 못 찾음 — 우리가 설정할 수 있는
  범위를 벗어난 구글 쪽 문제로 추정. 강석 결정으로 **이 연동 디버깅은 일단 중단**하고 에이전트
  본체 고도화에 집중하기로 함. 코드는 실패해도 경고만 찍고 계속 진행되도록 이미 try/catch로
  감싸져 있어서, 이 상태로도 슬랙 요약 전송은 정상 동작함.

  **전체 회원(282명) 첫 실행에서 발견된 버그**: `TEST_MEMBER_LIMIT` 없이 전체로 돌리니 2번째
  AI 호출에서 `max_tokens`(16000)를 다 써버려서 중간에 끊김 — 282명치 데이터가 5~20명 테스트
  때보다 훨씬 커서(캐시에 새로 쓴 토큰만 81429개) 처리할 내용이 늘어난 만큼 더 많은 토큰이
  필요했던 것. `max_tokens`를 16000 → **64000**으로 올려서 수정. 아직 강석 재검증 전.
- [ ] 4단계 — Slack 승인 워크플로 연결
- [ ] 5단계 — 스케줄링 (로컬 launchd 또는 GitHub Actions)

## 실행 방법 (로컬, 맥)

```bash
cd onemove-churn-agent
npm install
cp .env.example .env   # .env에 본인 ANTHROPIC_API_KEY 채워넣기 (2단계부터는 RESERVATION_* 값도)
npm run toy      # 1단계: 가짜 데이터
npm run step2    # 2단계: 진짜 원무브 데이터 (읽기 전용, 반드시 맥에서 실행)

# 프롬프트만 다듬어보는 개발 중에는 후보 수를 줄여서 싸고 빠르게 테스트:
TEST_MEMBER_LIMIT=20 npm run step2
# 최종 확인할 때는 TEST_MEMBER_LIMIT 없이(=282명 전체) 돌릴 것
```

실행이 끝나면 결과가 **구글 시트**에 기록되고 **슬랙**으로 요약이 전송됩니다 (CSV 파일 방식은
폐기 — 팀 전체가 접근 가능해야 한다는 요청으로 구글 시트로 전환).

- 구글 시트: 날짜/위험도/회원ID/회원명/요약/**연락완료(체크박스)** 컬럼. 직원이 연락한 뒤
  연락완료를 체크하면, 그 회원ID는 다음 실행부터 자동으로 신고 대상에서 빠집니다(코드에서
  필터링 — AI 판단에 맡기지 않아서 확실함).
- 슬랙: 위험도별 인원수 + high 등급 전체 명단을 전문 용어 없는 쉬운 문장으로 보여주고, 구글
  시트 링크를 함께 보냅니다. medium/low 전체 명단과 연락완료 체크는 시트에서.

두 기능 다 설정이 안 돼 있으면 경고만 출력하고 나머지는 정상 진행됩니다.

### 구글 시트 연결 (앱스 스크립트 방식, 최초 1회만)

구글 클라우드 콘솔, 서비스 계정, API 키 전부 필요 없습니다 — 슬랙 웹훅과 똑같은 방식(URL에
데이터를 보내는 것)으로 연결합니다. 전부 무료입니다.

1. 구글 드라이브에서 새 스프레드시트를 만듭니다.
2. 상단 메뉴에서 **확장 프로그램 → Apps Script**를 클릭합니다. 편집기 창이 새로 열립니다.
3. 편집기 안의 기본 코드(`function myFunction() {...}`)를 전부 지우고, 이 저장소의
   `onemove-churn-agent/apps-script/Code.gs` 파일 내용을 그대로 복사해서 붙여넣습니다.
4. 저장(플로피 디스크 아이콘 또는 cmd+S)합니다.
5. 오른쪽 위 파란색 **"배포" → "새 배포"** 버튼을 클릭합니다.
6. 유형 선택에서 톱니바퀴 아이콘을 클릭해 **"웹 앱"**을 선택합니다.
7. "다음 사용자 인증 정보로 실행" → **나**, "액세스 권한이 있는 사용자" → **모든 사용자**로
   설정하고 **배포**를 누릅니다.
8. 처음 배포할 때 구글이 권한 승인을 요청합니다 — 본인 계정으로 승인해주세요 ("고급" →
   "이동(안전하지 않음)" 문구가 떠도 본인이 만든 스크립트라 정상입니다).
9. 배포가 끝나면 **웹 앱 URL**이 나옵니다 (`https://script.google.com/macros/s/.../exec` 형태).
   이 URL을 `.env`의 `GOOGLE_APPS_SCRIPT_URL`에 붙여넣습니다.
10. 브라우저 주소창에 있는 스프레드시트 자체 URL(평소 보던 그 주소)을 `.env`의
    `GOOGLE_SHEET_VIEW_URL`에 붙여넣습니다 — 슬랙 메시지에 링크로 들어갈 값입니다.
11. 시트 탭 이름, 헤더 행, 연락완료 체크박스 서식은 스크립트가 처음 호출될 때 자동으로
    만들어줍니다 — 직접 만들 필요 없습니다.

스크립트 코드를 나중에 수정하면(예: 컬럼 추가), 편집기에서 저장한 뒤 "배포 → 배포 관리 →
연필 아이콘 → 새 버전"으로 다시 배포해야 반영됩니다 (URL은 그대로 유지됨).

## 참고

- 1단계는 어디서든 실행 가능하지만, 2단계부터는 원무브 예약 API(`api-reservation.onemove.co.kr`)에
  접속해야 해서 반드시 로컬 Claude Code(맥)에서 실행 — Cowork/클라우드 세션은 해당 도메인이
  allowlist에 없어 직접 호출이 막혀 있습니다 (`onemove-reservation-api` 스킬 참고).
- `RESERVATION_API_USERNAME`/`PASSWORD` 값은 기존 `onemove-crm-agent/.env` 또는
  `onemove-daily-report/.env`에 이미 있는 값을 그대로 복사해서 씁니다.

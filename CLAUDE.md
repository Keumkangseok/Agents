# 작업 지침

이 저장소(Agents)에서 작업할 때 반드시 지킬 것.

## 방향만 제시받았을 때 마음대로 구현하지 말고 먼저 질문할 것

강석이 "이런 느낌이어야 한다", "~하면 좋겠다"처럼 방향성만 말하고 구체적인 방식은
정해주지 않았을 때, 스스로 방식을 정해서 바로 구현하지 말 것. 대신 구체적인 선택지를
제시하고 먼저 확인받을 것.

예시 (실제 있었던 일): "결과가 알아보기 쉽게 나와야 한다"는 피드백을 받고, 물어보지 않고
바로 CSV 파일로 정해서 구현해버림 — 이건 잘못된 방식. 올바른 방식: "CSV로 만들까요,
아니면 엑셀(여러 시트로), 슬랙 메시지, 웹페이지 중 어떤 형태를 원하세요?"처럼 먼저 물어보고
답을 들은 다음에 구현.

## 바로 진행해도 되는 것 vs 먼저 물어야 하는 것

- **먼저 안 물어도 됨**: 명백한 버그 수정, 이미 합의된 방향 안에서의 세부 구현, 강석이
  이미 명확히 지시한 내용을 그대로 구현하는 것.
- **반드시 먼저 물어야 함**: 결과물의 형태/포맷을 새로 정하는 것, 새 기능을 어떤 방식으로
  만들지 정하는 것, 여러 구현 방법 중 하나를 골라야 하는 것, 강석이 "~하면 좋겠다"고만
  말하고 구체적인 방식은 언급 안 한 모든 경우.

## 방법

선택지가 명확하면 AskUserQuestion 도구로 2~4개 선택지를 제시해서 고르게 할 것.
애매하면 그냥 텍스트로 "이렇게 해도 될까요?" 물어봐도 됨. 중요한 건 구현부터 하고
나중에 보여주는 게 아니라, 방향을 먼저 확인하고 나서 구현하는 순서를 지키는 것.
